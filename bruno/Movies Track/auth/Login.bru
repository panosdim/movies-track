meta {
  name: Login
  type: http
  seq: 5
}

post {
  url: {{url}}/login
  body: json
  auth: inherit
}

body:json {
  {
      "email": "test@test.com",
      "password": "testpass"
  }
}

script:post-response {
  test("Status code is 200", function () {
      expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
}
