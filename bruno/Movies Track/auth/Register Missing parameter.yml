info:
  name: Register Missing parameter
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/register"
  body:
    type: json
    data: "{\r

      \    \"email\": \"test@test.com\",\r

      \    \"password\": \"testpass\",\r

      \    \"lastName\": \"last name\"\r

      }\r\n"
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        test("Status code is 422", function () {
            expect(res.getStatus()).to.equal(422);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
