meta {
  name: Set Watched
  type: http
  seq: 5
}

post {
  url: {{url}}/movies/watched/{{movie_id}}
  body: json
  auth: inherit
}

script:post-response {
  test("Status code is 200", function () {
      expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
}
