info:
  name: Add Movie Missing parameter
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/movies"
  body:
    type: json
    data: "{\r\n    \"movieId\": 1225\r\n}"
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: "test(\"Status code is 400\", function () {\r

        \    expect(res.getStatus()).to.equal(400);\r

        });"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
