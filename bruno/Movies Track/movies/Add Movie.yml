info:
  name: Add Movie
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/movies"
  body:
    type: json
    data: "{\r

      \    \"movieId\": 302946,\r

      \    \"title\": \"The Accountant\",\r

      \    \"poster\": \"/dTGECn8He16tZeBHQTLf6rVydE8.jpg\"\r

      }"
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: "if (res.getSize().body > 0) {\r

        \    var jsonData = res.getBody();\r

        \    if (jsonData && jsonData.id) {\r

        \        bru.setVar(\"movie_id\", jsonData.id);\r

        \    }\r

        }\r

        \r

        test(\"Status code is 200\", function () {\r

        \    expect(res.getStatus()).to.equal(200);\r

        });"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
